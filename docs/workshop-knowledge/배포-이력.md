# wiki-agent 배포 이력

---

## v0.2.0 — 2026-04-30 (예정)

**주요 변경:**
- RouterEvalTest 추가 (자동화된 검색 품질 측정)
- 워크숍 레포 (`wiki-agent-workshop`) 공개
- 세션 발표 자료 업데이트

**배포 방법:** `./gradlew shadowJar && docker build -t wiki-agent:0.2.0 .`

---

## v0.1.3 — 2026-04-28

**주요 변경:**
- DATE_AFTER / DATE_BEFORE CQL 필터 추가
- Router 프롬프트 날짜 포맷 변환 유형 추가 (6번째 유형)
- GitHub Wiki 검색 연동 완료

**핫픽스:** Confluence 토큰 만료 시 명확한 에러 메시지 출력

---

## v0.1.2 — 2026-04-24

**주요 변경:**
- cleanQuery() 도입 — CQL 특수문자 사전 정리
- Router LLM SYNONYMS 추출 (5가지 유형)
- 병렬 fallback (coroutineScope + async)

**성능:** Recall@5 40.2% → 83.0% 달성

---

## v0.1.1 — 2026-04-20

**주요 변경:**
- Slack Socket Mode 안정화
- 다중 스페이스 지원 (confluence.spaces 리스트)
- 응답 footer에 출처 URL 표시

**버그 수정:** AppMentionEvent 중복 처리 이슈 수정

---

## v0.1.0 — 2026-04-14 (첫 배포)

**포함 기능:**
- Confluence CQL 검색 (단일 스페이스)
- Slack Socket Mode 연동
- Claude Haiku 기반 답변 생성
- 기본 tool calling (confluenceSearch 1개)

**알려진 한계:**
- Recall@5 40% — 동의어 검색 없음
- 응답 시간 8~15초 — 순차 처리

---

## 롤백 방법

```bash
# 이전 버전으로 롤백
docker pull wiki-agent:<이전 버전>
docker stop wiki-agent && docker rm wiki-agent
docker run -d --name wiki-agent wiki-agent:<이전 버전>
```

## 모니터링

- 로그: `docker logs wiki-agent -f`
- 슬랙 알림: `#wiki-agent-alerts` 채널
